
  # Tool Access Management App

  This is a code bundle for Tool Access Management App. The original project is available at https://www.figma.com/design/w85otBbfWRAGjzbV5thmwk/Tool-Access-Management-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # gatekeepr
